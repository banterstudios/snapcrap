import {
  StyleSheet
} from 'react-native'

import { textLight } from 'styles/colors.styles'

export default StyleSheet.create({
  inputComponent: {
    textAlign: 'center',
    color: textLight
  }
})
