import {
  StyleSheet
} from 'react-native'

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    width: 100,
    height: 100
  },
  logo: {
    width: '100%',
    resizeMode: 'contain',
    flex: 1
  }
})
